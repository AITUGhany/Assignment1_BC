{
  "name": "bcht2-assignment-1-bank",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "gas": "REPORT_GAS=true hardhat test",
    "node": "hardhat node",
    "deploy:factory": "hardhat run scripts/deployFactory.js --network localhost",
    "deploy:uups": "hardhat run scripts/deployUUPS.js --network localhost",
    "deploy:assembly": "hardhat run scripts/deployAssembly.js --network localhost",
    "deploy:gas": "hardhat run scripts/deployGasWorkshop.js --network localhost",
    "slither": "slither . --filter-paths node_modules,test,artifacts,cache"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "@openzeppelin/contracts-upgradeable": "^5.0.2",
    "@openzeppelin/hardhat-upgrades": "^3.1.0",
    "hardhat": "^2.28.6",
    "hardhat-gas-reporter": "^1.0.10"
  }
}
